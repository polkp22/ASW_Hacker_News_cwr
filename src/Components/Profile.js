import React from 'react';

const Profile=()=>{
    return(
         // make a pop up window to show the user's profile
        <div className="profile">
            <button className="btn btn-primary">Show Profile</button>
        </div>
    )
}

export default Profile;